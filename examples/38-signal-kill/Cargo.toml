[workspace]
resolver = "2"
members = ["signal-kill", "signal-kill-common"]
exclude = ["signal-kill-ebpf"]
[workspace.package]
license = "Apache-2.0"
edition = "2021"
[profile.release]
lto = true
codegen-units = 1
