[workspace]
resolver = "2"
members = ["pg-probe", "pg-probe-common", "pg-probe-ebpf"]
default-members = ["pg-probe", "pg-probe-common"]
[workspace.package]
license = "Apache-2.0"
edition = "2021"
[profile.release]
lto = true
codegen-units = 1
